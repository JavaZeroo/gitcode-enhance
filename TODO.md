# TODO

- [ ] 验证码 SDK 拦截：三个 SDK 是 SSR HTML 里的 `<script defer>`，用 file:// 开发加载器时油猴注入晚于 `<head>` 解析，Chrome 不会取消已经 prepare 的脚本，实测拦不住（面板「本页统计」会如实显示「未拦住」）。需要用 GreasyFork/GitHub 正式安装版验证注入时机是否够早；如果也拦不住，考虑把这一项从极致级预设里去掉。
- [ ] 主线程长任务统计：PerformanceObserver 已注册成功，但在后台标签页里测不到数据，需要在前台标签页确认。
- [ ] PR 评论自动展开：目前是打开页面就全展开，考虑加开关/改成手动触发
- [ ] 拦截规则（AI 接口 / 营销请求）是手写的 URL 关键字，gitcode 改版后可能漏/误拦，需要定期复核
- [ ] 去广告目前靠 class / 图片路径匹配，可能还有别的营销位没覆盖
- [ ] 设置面板改动后需要刷新页面才生效，CSS 类的开关理论上可以即时生效
